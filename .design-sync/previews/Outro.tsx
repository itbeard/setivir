import { Outro } from 'setivir'

// The closing screen: an ornament divider, the "thank you for listening"
// display-serif heading, a follow link, a back-to-top button, and the
// copyright line. In-flow `.section`, single-card viewport (1000x820).

export function Default() {
  return <Outro />
}
