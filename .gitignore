# dependencies
node_modules
.pnp
.pnp.js

# build output
dist
dist-ssr
*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor / OS
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# generated asset symlinks (recreated from ../songs and ../thumbnails)
public/songs
public/thumbnails

# design-sync (claude.ai/design) — staged scripts, build output, machine state.
# The durable inputs under .design-sync/ (config.json, NOTES.md, conventions.md,
# ds-entry.tsx, base.css, shims/, tsconfig.esbuild.json, previews/, overrides/)
# ARE committed; only the generated/staged paths below are ignored.
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
